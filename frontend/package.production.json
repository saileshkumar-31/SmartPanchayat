{
  "name": "smartpanchayat-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "serve": "serve -s build -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
