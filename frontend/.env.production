# Frontend production environment
# Public base path of the app. Use "/" for root deployments.
VITE_BASE=/

# Backend API base (proxy is not used in production builds unless you keep it).
VITE_API_URL="http://localhost:8010/api"
